[Unit]
Description=Battery monitor daemon
Documentation=man:batsignal(1)
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/batsignal -p -e -a Battery -w 20 -c 10 -d 5 -D 'systemctl hibernate'
Restart=on-failure
RestartSec=1

[Install]
WantedBy=graphical-session.target
