---
name: rust
description: Rust lint-gate and idiom standard — clippy -D warnings with the pedantic group and cherry-picked restriction lints (unwrap_used, expect_used, dbg_macro, todo) in Cargo.toml, smallest-scope documented #[allow] suppressions only, plus the un-lintable named idioms (ownership over clone, thiserror in libraries / anyhow in binaries). Use whenever editing, testing, or committing Rust code, when clippy is red, or when setting up lints for a new crate.
---

Read `~/.config/claude/contexts/rust.md` and follow it. That file is the single source of truth for this skill.
