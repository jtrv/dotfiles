#!/bin/sh

GREETING="$XDG_DATA_HOME"/greeting

{
	iching &&
		if [ "$(date +%a)" = "Mon" ]; then
			nws-discussion
		else
			nws-forecast
		fi
} >"$GREETING" &&
	kak -e "kakpipe -n greeting -- bat $GREETING"
