#!/bin/sh

pulsemixer --toggle-mute

MUTE_VALUE=$(pulsemixer --get-mute)

if [ "$MUTE_VALUE" -eq "1" ]; then
	MUTE_STATUS="On"
fi

if [ "$MUTE_VALUE" -eq "0" ]; then
	MUTE_STATUS="Off"
fi

notify-send -t 2500 "Mute Toggled $MUTE_STATUS"
