#!/bin/sh

if [ $# -eq 0 ]; then
	pulsemixer --max-volume 100 --change-volume +5
fi

if [ $# -eq 1 ]; then
	pulsemixer --max-volume 100 --change-volume +"$1"
fi

VOLUME_LEVEL=$(pulsemixer --get-volume | cut -d ' ' -f 1)
notify-send -t 200 "Volume + $VOLUME_LEVEL%"
